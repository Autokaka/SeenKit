{
  "private": true,
  "name": "@seen-kit/example",
  "version": "1.0.0",
  "description": "A SeenKit example project.",
  "scripts": {
    "build": "seen tsb -o demo.seen"
  },
  "devDependencies": {
    "@seen-kit/seen": "workspace:^"
  }
}
